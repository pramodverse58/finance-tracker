# Only needed if you serve the frontend separately from the backend
# (e.g. a production build not using the Vite dev proxy).
# During `npm run dev`, requests to /api are proxied to localhost:8080 automatically.
VITE_API_BASE_URL=http://localhost:8080/api
